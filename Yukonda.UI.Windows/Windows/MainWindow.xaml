<Window x:Class="Yukonda.UI.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:svg="http://sharpvectors.codeplex.com/runtime/"
        xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
        mc:Ignorable="d" Title="Yukonda" Height="450" Width="800"
        Icon="pack://application:,,,/favicon.ico"
        xmlns:local="clr-namespace:Yukonda.UI.Windows"
        >
    <Window.Resources>
        <Style x:Key="toolBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="4"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter Content="{TemplateBinding Content}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#20000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="appMenuExpanderTb" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Left"
                    Width="220"
                    Margin="0,0,0,0"
                    Background="LightGray"
                    
                    >
            <TextBlock FontSize="30"
           FontWeight="Bold"
           Text="Yukonda"
                       Margin="10,70,10,0"/>
            <ListBox ItemsSource="{Binding Connections}"
                     SelectedItem="{Binding Connection}"
                     Background="Transparent"
                     Foreground="#FFFFFF"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     BorderThickness="0">
                <ListBox.Resources>
                    <Style TargetType="Expander">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Expander">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Name="ContentRow" Height="0"/>
                                        </Grid.RowDefinitions>
                                        <Border 
                                            Padding="5"
              Name="Border" 
              Height="48"                             
              Grid.Row="0" 
              BorderThickness="1" 
              CornerRadius="2,2,0,0" >
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="20" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <ToggleButton Opacity="2"
                                                              Style="{DynamicResource appMenuExpanderTb}"
                  IsChecked="{Binding Path=IsExpanded,Mode=TwoWay,
                                      RelativeSource={RelativeSource TemplatedParent}}"
                                                              >
                                                    
                                                </ToggleButton>
                                                <ContentPresenter 
                  Grid.Column="1"
                  Margin="0" 
                  ContentSource="Header" 
                  RecognizesAccessKey="True" />
                                            </Grid>
                                        </Border>
                                        <Border 
              Name="Content" 
              Grid.Row="1" 
              BorderThickness="1,0,1,1" 
              CornerRadius="0,0,2,2" >
                                            <ContentPresenter Margin="0" />
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsExpanded" Value="True">
                                            
                                        </Trigger>

                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}"
                                            MinHeight="32"
                                            CornerRadius="8"
                                            Margin="5">
                                        <ContentPresenter Content="{TemplateBinding Content}"
                                                          ContentTemplate="{TemplateBinding ContentTemplate}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Purple"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#20000000"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.Resources>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="10,0">
                            <svgc:SvgViewbox Width="18"
Height="18"
Source="{Binding DbProvider.Icon}"/>
                            <TextBlock Text="{Binding ConnectionName}"
                                               Foreground="#FFFFFF"
               VerticalAlignment="Center"
               Margin="5,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <Grid>
            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                          >
                <StackPanel Margin="10,70,10,10">
                    <TextBlock FontSize="30"
           FontWeight="Bold"
           Text="Tables"/>
                    <ListBox ScrollViewer.VerticalScrollBarVisibility="Disabled"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             SelectedItem="{Binding Connection.CurrentTable}"
                             ItemsSource="{Binding Connection.Tables}"
                             Background="Transparent"
                             BorderThickness="0">
                        
                    </ListBox>
                    <TextBlock FontSize="30"
                               FontWeight="Bold"
                               Text="Preview Data"/>
                    <DataGrid ScrollViewer.VerticalScrollBarVisibility="Disabled" 
                              BorderThickness="0"
                              ItemsSource="{Binding Connection.DataTable}"></DataGrid>
                </StackPanel>
            </ScrollViewer>
            <Border CornerRadius="6"
                    Width="320"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"
                    BorderThickness="1"
                    BorderBrush="#20000000"
                    Margin="8"
                    Background="#FFFFFF"
                    Height="36">
                <DockPanel LastChildFill="True"
                           Margin="2">
                    <Button Style="{DynamicResource toolBarButton}"
                            Command="{Binding CreateNewConnectionCommand}"
                            ToolTip="Создать новое подключение">
                        <svgc:SvgViewbox Width="12"
                                         Height="12"
                                         Source="C:\Users\student\Downloads\plus_3891l2tydvp9.svg"></svgc:SvgViewbox>
                    </Button>
                    <TextBlock VerticalAlignment="Center"
                               Margin="10,0"
                               Text="{Binding Connection.ConnectionName, FallbackValue=Нет активных подключений}"/>
                    <Grid></Grid>
                </DockPanel>
            </Border>
            <Border CornerRadius="8"
                    DataContext="{Binding CreateConnectionWindowViewModel}"
                    Visibility="{Binding IsShown, Converter={local:VisibilityConverter}}"
                    HorizontalAlignment="Left"
                    Margin="8"
        Background="#FFFFFF"
        BorderBrush="#40000000"
        BorderThickness="1"
                    VerticalAlignment="Top"
                    Width="320"
        Padding="20">
                <Border.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="6"
                        Height="24"
                        Background="{TemplateBinding Background}">
                                        <TextBlock Text="{TemplateBinding Content}"
                               TextAlignment="Center"
                               VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsEnabled" Value="True">
                                            <Setter Property="Background" Value="#20000000"/>
                                        </Trigger>
                                        <Trigger Property="IsDefault" Value="True">
                                            <Setter Property="Background" Value="DodgerBlue"/>
                                            <Setter Property="Foreground" Value="#FFFFFF"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="CornflowerBlue"/>
                                            <Setter Property="Foreground" Value="#FFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Border.Resources>
                <StackPanel Orientation="Vertical"
                HorizontalAlignment="Center"
                >
                    <TextBlock TextAlignment="Center"
Margin="0,-8,0,20"
Text="СОЗДАТЬ НОВОЕ ПОДКЛЮЧЕНИЕ"
FontWeight="Bold"/>
                    <Image Source="{DynamicResource appIconDrawingImage}"
               Width="52"/>
                    
                    <TextBlock Text="Тип подключения:"/>
                    <DockPanel LastChildFill="True">
                        <svgc:SvgViewbox Width="32"
                             Height="32"
                   Source="{Binding DbProvider.Icon}"/>

                        <ComboBox Margin="2" ItemsSource="{Binding Providers}"
      SelectedItem="{Binding DbProvider}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </DockPanel>
                    <DockPanel LastChildFill="True">
                        <StackPanel Width="70"
                        DockPanel.Dock="Right"
                        Margin="2">
                            <TextBlock Text="Порт:"/>
                            <TextBox Text="{Binding Port}"/>
                        </StackPanel>
                        <StackPanel Margin="2">
                            <TextBlock Text="Адрес:"/>
                            <TextBox Text="{Binding Host}"/>
                        </StackPanel>
                    </DockPanel>
                    <StackPanel Margin="2">
                        <TextBlock Text="Пользователь:"/>
                        <TextBox Text="{Binding Username}"/>
                    </StackPanel>
                    <StackPanel Margin="2">
                        <TextBlock Text="Пароль:"/>
                        <TextBox Text="{Binding Password}"/>
                    </StackPanel>
                    <StackPanel Margin="2">
                        <TextBlock Text="База данных:"/>
                        <TextBox Text="{Binding Database}"/>
                    </StackPanel>
                    <Button IsDefault="True"
                Margin="0,10"
                Command="{Binding TryConnectCommand}">Подключиться</Button>
                    <Button Command="{Binding CancelConnectCommand}" >Отменить</Button>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>